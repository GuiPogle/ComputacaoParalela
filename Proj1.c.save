#include <stdio.h>
#include <gmp.h>

long double euler();

int main(void) 
{
    printf("%.1000f", euler());
    return 0;
}

mpf_t euler()
{
    mpf_init2(e, precision);
    mpf_init2(temp, precision);
    mpf_init2(fat, precision);

    mpf_set_d(e, 1.0);
    mpf_set_d(temp, 1.0);
    mpf_set_d(fat, 1.0);

    for(int i = 1; i <= 100; i++)
    {
         mpf_mul_ui(fat, fat, i);
        mpf_div_ui(temp, temp, fat);
        mpf_add(e, e, temp);
    }
    return e;
}#include <stdio.h>
#include <gmp.h>

long double euler();

int main(void) 
{
    printf("%.1000f", euler());
    return 0;
}

mpf_t euler()
{
    mpf_init2(e, precision);
    mpf_init2(temp, precision);
    mpf_init2(fat, precision);

    mpf_set_d(e, 1.0);
    mpf_set_d(temp, 1.0);
    mpf_set_d(fat, 1.0);

    for(int i = 1; i <= 100; i++)
    {
         mpf_mul_ui(fat, fat, i);
        mpf_div_ui(temp, temp, fat);
        mpf_add(e, e, temp);
    }
    return e;
}
